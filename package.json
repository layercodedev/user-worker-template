{
  "name": "user-worker-template",
  "version": "1.0.0",
  "description": "A template for user worker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Layercode",
  "dependencies": {
    "openai": "^4.56.0"
  }
}
